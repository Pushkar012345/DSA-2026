/**
 * @param {number} n
 * @return {number[]}
 */
var countBits = function(n) {
    const ans=new Array(n+1).fill(0);

    for(let i=1;i<=n;i++)
    {
        const halfBits=ans[i>>1];
        const lastBit= i&1;

        ans[i]=halfBits+lastBit;
    }

    return ans;
};
