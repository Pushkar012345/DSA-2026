/**
 * @param {number[]} gain
 * @return {number}
 */
var largestAltitude = function(gain) {
    let currentaltitude=0;
    let maxalt=0;

    for(let i=0;i<gain.length;i++)
    {
        currentaltitude+=gain[i];
        maxalt=Math.max(maxalt,currentaltitude);
    }

    return maxalt;
};
