/**
 * @param {number[]} nums
 * @return {number}
 */
var missingNumber = function(nums) {
    let n=nums.length;
    let ans=n;

    for(let i=0;i<n;i++)
    {
        ans^=i;
        ans^=nums[i];
    }

    return ans;
};
